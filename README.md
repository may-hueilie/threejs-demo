# threejs-demo

Scrollable 3D Animation with Three.js

To initialise project:

```npm init @vitejs/app```

```npm install three```

To run the project in local:

```npm run dev```

http://localhost:3000/
